# 学迹 BookMind 源码审阅说明

## 项目定位

学迹 BookMind 是面向教材学习场景的智能体应用。系统将 PDF 资料解析、知识图谱、检索增强问答、练习生成、提示、误区诊断和掌握状态管理整合为一个可在本机运行的学习空间。

## 代码入口

- 后端应用入口：`backend/bookmind/api/app.py`
- 模型路由：`backend/bookmind/llm/router.py`
- 教材问答：`backend/bookmind/services/book_qa.py`
- 学习任务与提示：`backend/bookmind/services/task_service.py`
- 检索融合：`backend/bookmind/retrieval/fusion.py`
- 前端入口：`frontend/src/App.tsx`
- 前端请求层：`frontend/src/api/client.ts`

## 模型配置

- 问答主模型：`qwen-chat`
- 问答回退模型：`glm-5.3-flash`
- 向量模型：`qwen3-embedding`
- 重排模型：`qwen3-reranker`

API 地址、模型名和比赛演示密钥位于项目根目录 `.env`。聊天请求采用有界超时和自动回退，检索失败时保守返回原文引用，不伪造资料内容。

## 本地运行

Windows PowerShell：

```powershell
py -3.11 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
powershell -ExecutionPolicy Bypass -File .\start.ps1
```

启动后访问 `http://127.0.0.1:18765/ui/`。

## 验证

后端测试位于 `tests`，前端可在 `frontend` 中执行类型检查和构建。源码包不包含个人学习数据、教材文件、虚拟环境、依赖缓存、历史打包产物或项目构建计划。
